{
	"name": "@645/og-image-core",
	"version": "0.1.0",
	"description": "Core package for OG image generation",
	"main": "src/index.ts",
	"types": "src/index.ts",
	"scripts": {
		"dev": "bun --watch src/index.ts",
		"build": "bun build src/index.ts --outdir dist",
		"check": "bunx tsc --noEmit"
	},
	"dependencies": {
		"hono": "catalog:"
	},
	"devDependencies": {
		"typescript": "^5.0.0"
	},
	"peerDependencies": {
		"react": "^19.0.0",
		"@types/react": "^19.0.0"
	}
}
