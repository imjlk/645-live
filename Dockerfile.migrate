FROM oven/bun:1.3.10

WORKDIR /app

RUN apt-get update \
	&& apt-get install -y --no-install-recommends ca-certificates \
	&& rm -rf /var/lib/apt/lists/*

COPY . .

RUN bun install --frozen-lockfile

CMD ["bun", "run", "--cwd", "pages/www", "db:migrate:remote"]
